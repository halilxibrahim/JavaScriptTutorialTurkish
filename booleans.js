/*
Booleans (Boole Değerler)
JavaScript'te, yalnızca iki değeri olan Boole (Boolean) veri türü bulunur: true (doğru) ve false (yanlış). Boole değerler genellikle koşullu ifadelerde ve karar yapılarında kullanılır. Örneğin:
*/

let isLogged = true;
let isOpen = false;

if (isLogged) {
    console.log("Kullanıcı oturum açmış.");
} else {
    console.log("Kullanıcı oturum açmamış.");
}

/* */